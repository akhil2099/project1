pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build and Deploy') {
            steps {
                script {
                    docker.image('nginx').pull()
                    docker.image('nginx').withRun('-p 8080:80') {
                        sh 'cp index.html /usr/share/nginx/html/'
                    }
                }
            }
        }
    }
}
