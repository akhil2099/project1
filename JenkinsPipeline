pipeline {
    agent any

    stages {
        stage('Run Nginx Container') {
            steps {
                script {
                    sh 'docker run -d -p 80:80 --name my-nginx -v /home/akhil/pipeline1:/usr/share/nginx/html nginx'
                }
            }
        }
    }
}
